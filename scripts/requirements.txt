# PDF Editor 가 실제로 import 하는 것만 담았다 (2026-08-13 정리, dry-run 검증).
#
# 왜 requirements.lock 을 쓰지 않는가
#   그 파일은 2026-07-20 '환경 전체' 스냅숏이라 wandb·polars·numba 처럼 이 도구와
#   무관한 패키지가 섞여 있고, numba(numpy<2.5) 와 고정된 numpy==2.5.0 이 정면으로
#   충돌해 **그 파일로는 환경을 다시 만들 수 없다**(ResolutionImpossible 실측).
#   스냅숏이지 빌드 가능한 명세가 아니다. 이력 참고용으로만 남겨 둔다.
#
# ★★ onnxruntime 은 반드시 -directml 판이어야 한다
#   일반 onnxruntime 이 같이 깔리면 같은 onnxruntime/ 폴더를 덮어써 iGPU 가속이
#   죽는다(과거 실제 사고). dry-run 으로 확인한 유입 경로는
#       optimum → optimum-onnx → onnxruntime
#   이라 optimum 을 여기서 뺐다. setup_env.py 가 설치 후 DirectML 을 검증한다.
#
# ★ mfr_rebuild/ (MFR 모델 재제작 일회성 유틸) 는 onnx·optimum 이 더 필요하다.
#   그 작업을 할 때만 따로 깔 것 — 평상시 변환에는 쓰이지 않는다:
#       venv\Scripts\python -m pip install onnx optimum
#   (그러면 일반 onnxruntime 이 들어와 가속이 꺼질 수 있으니, 끝난 뒤
#    python -m pip install --force-reinstall onnxruntime-directml 로 되돌릴 것.)

pillow==12.2.0
numpy==2.5.0
pypdfium2==5.9.0
pix2text==1.1.6
transformers==4.57.6
torch==2.12.0
onnxruntime-directml==1.24.4
